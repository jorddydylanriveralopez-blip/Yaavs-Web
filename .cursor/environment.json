{
  "name": "Yaavs-Web",
  "install": "npm config set prefix \"$HOME/.npm-global\" && npm install -g netlify-cli@27.4.2",
  "terminals": [
    {
      "name": "netlify-dev",
      "command": "export PATH=\"$HOME/.npm-global/bin:$PATH\" && cd /workspace && netlify dev --offline"
    }
  ],
  "ports": [8888]
}
